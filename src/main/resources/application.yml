server.port: 8989
server.servlet.context-path: /bank-api

spring.security.user.name: bankapp
spring.security.user.password: changeit

# The H2 console is disabled by default. Run with --spring.profiles.active=local
# (see application-local.yml) to enable it for local development only.
spring:
  h2:
    console:
      enabled: false
